%ol
  - @terms.each do |term|
    %li
      - if term.revisable?
        = link_to term.content, [ :edit, term ]
      - else
        %p= link_to truncate(term.content), term
        %p= "#{term.signatures.count} of #{term.participants.count} signatures." 
      %time= term.created_at

          
